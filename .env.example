# Copy to .env.local for local overrides (optional).
# Vercel / Netlify: set the same keys in the project Environment Variables UI.

# Example future LLM API base (not used by the local demo replies yet)
# VITE_API_BASE_URL=https://api.example.com
