/**
 * #333A/#335A HomeHero — superseded by HomeExperience (#335B).
 * Kept as a thin re-export for import stability / legacy tests that only check presence.
 */

export { HomeExperience as HomeHero } from './home/HomeExperience'
